[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "midscene-android"
version = "0.2.0"
description = "AI-powered Android automation via Midscene.js, bridged into Python"
readme = "README.md"
requires-python = ">=3.9"
license = { text = "MIT" }
keywords = ["android", "automation", "testing", "ai", "midscene"]
classifiers = [
    "Development Status :: 3 - Alpha",
    "Intended Audience :: Developers",
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3.9",
    "Programming Language :: Python :: 3.10",
    "Programming Language :: Python :: 3.11",
    "Programming Language :: Python :: 3.12",
    "Topic :: Software Development :: Testing",
]
dependencies = [
    "python-dotenv>=1.2.1",
    "requests>=2.28",
    "setuptools>=82.0.1",
]

[project.optional-dependencies]
dev = [
    "pytest>=8.0",
    "pytest-mock",
    "build",
    "twine",
    "ruff",
]

[tool.pytest.ini_options]
markers = [
    "device: tests that require a real Android device connected via ADB and a valid AI Key",
]

[tool.ruff]
line-length = 88
target-version = "py39"

[tool.ruff.lint]
select = [
    "E",   # pycodestyle errors
    "F",   # pyflakes
    "I",   # isort
    "UP",  # pyupgrade
    "B",   # flake8-bugbear
]
ignore = []

[tool.ruff.lint.isort]
known-first-party = ["midscene_android"]

[tool.setuptools.packages.find]
where = ["."]
include = ["midscene_android*"]

[tool.setuptools.package-data]
midscene_android = [
    # 平台 Node 可执行文件（构建时只保留目标平台的文件，其余已删除）
    "_node_driver/bin/node-linux-x64",
    "_node_driver/bin/node-linux-arm64",
    "_node_driver/bin/node-darwin-x64",
    "_node_driver/bin/node-darwin-arm64",
    "_node_driver/bin/node-win32-x64.exe",
    "_node_driver/bin/node.cmd",          # Windows PATH shim
    # 内置 npm（npm install 时使用，无需系统 npm）
    "_node_driver/npm/**",
    # Node.js RPC 服务源码
    "_node_driver/src/package.json",
    "_node_driver/src/service.js",
]
